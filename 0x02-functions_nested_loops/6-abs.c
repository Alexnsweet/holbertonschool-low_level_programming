#include "holberton.h"
/**
 * _abs - computes absolute value of a number
 * @value: absolute value
 *
 * Return: Something
 */
int _abs(int value)
{

	if (value < 0)
	{
		value = value * -1;
		return (value);
	}

		return (value);
}
