#include "holberton.h"
#include <stdio.h>
/**
 * main - Entry Point
 * @argc: argument size
 * @argv: string array of arguments
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
	{
	printf("%s\n", *argv);
	}
return (0);
}
